# 🧪 Инструкция по тестированию расширения

Перед установкой расширения в браузер вы можете проверить все функции с помощью тестовых HTML файлов.

## 📁 Тестовые файлы

Созданы 4 тестовых файла:

1. **test-all.html** - Полный автоматический тест всех функций (рекомендуется начать с него)
2. **test-api.html** - Тест подключения к API openexchangerates.org
3. **test-formulas.html** - Тест парсинга формул курсов
4. **test-price-parsing.html** - Тест парсинга и конвертации цен

## 🚀 Как запустить тесты

### Вариант 1: Открыть в браузере (рекомендуется)

1. Откройте файл `test-all.html` в браузере (просто дважды кликните на файл)
2. Тесты запустятся автоматически
3. Проверьте результаты - все должно быть ✅

### Вариант 2: Через локальный сервер

Если у вас установлен Python:
```bash
python -m http.server 8000
```
Затем откройте в браузере: `http://localhost:8000/test-all.html`

## ✅ Что проверяют тесты

### test-all.html (полный тест)
- ✅ Подключение к API openexchangerates.org
- ✅ Получение и расчет курсов валют (HUF→EUR, EUR→RSD)
- ✅ Парсинг формул (например, "/392*0.917")
- ✅ Парсинг цен (например, "15 990 Ft", "129,99 €")
- ✅ Логика обновления курсов раз в 12 часов

### test-api.html
- Проверяет подключение к API
- Показывает полученные курсы
- Демонстрирует примеры конвертации

### test-formulas.html
- Проверяет функцию `parseFormula()` из popup.js
- Тестирует различные форматы формул
- Позволяет ввести свою формулу для проверки

### test-price-parsing.html
- Проверяет функцию `parseAndConvertPrice()` из content.js
- Тестирует парсинг цен в форматах "15 990 Ft" и "129,99 €"
- Позволяет ввести свою цену для проверки

## 🔍 Что делать, если тесты не проходят

1. **API тест не проходит:**
   - Проверьте интернет-соединение
   - Убедитесь, что APP_ID правильный (в test.js)
   - Проверьте доступность openexchangerates.org

2. **Формулы не парсятся:**
   - Проверьте формат формулы (должен начинаться с оператора: /, *, +, -)
   - Примеры правильных формул: "/392*0.917", "*117.5"

3. **Цены не парсятся:**
   - Формат должен быть: "число Ft" или "число €"
   - Примеры: "15 990 Ft", "129,99 €"

## 📦 После успешного тестирования

Если все тесты пройдены ✅, расширение готово к установке:

1. Откройте Chrome/Edge
2. Перейдите в `chrome://extensions/` или `edge://extensions/`
3. Включите "Режим разработчика"
4. Нажмите "Загрузить распакованное расширение"
5. Выберите папку `HufPriceConverter`

## ⚠️ Важно

- Тестовые файлы (`test-*.html`) можно удалить после установки расширения
- Они не нужны для работы расширения
- Они только для проверки перед установкой

